# Templates

Document and repository templates for starting new frostyard work. Copy a
template out of this directory into the repo that needs it — don't link to it
in place, so downstream repos stay self-contained.

*(none yet — add the first one when a starting point repeats across repos)*
